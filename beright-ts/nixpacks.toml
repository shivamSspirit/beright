[phases.setup]
nixPkgs = ["nodejs_22"]

[variables]
NODE_VERSION = "22"
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

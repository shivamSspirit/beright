# ---- GENERATED BY QEDGEN ---- spec-hash:46515066f593e63d

[package]
name = "berightcapital"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
default = []
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
client = []
debug = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { version = "0.32.1", features = ["init-if-needed"] }
anchor-spl = { version = "0.32.1", default-features = false, features = ["token", "token_2022"] }
# Solana CLI 2.1 bundles Cargo 1.79; pin transitive proc-macro tooling to
# versions that do not require edition 2024.
indexmap = "=2.2.6"
proc-macro-crate = "=3.1.0"
zeroize = "=1.7.0"
zeroize_derive = "=1.4.3"
unicode-segmentation = "=1.12.0"
solana-instruction = "=2.2.1"
solana-pubkey = "=2.2.1"

/**
 * GSAP Animation Hooks
 *
 * Re-export all GSAP hooks for convenient importing
 */

export { useScrollReveal, type ScrollRevealOptions } from './useScrollReveal';
export { useStagger, type StaggerOptions } from './useStagger';

// Re-export GSAP config for direct access
export * from '@/lib/gsap-config';
